function openSubDepartment(department) {
    alert(`Welcome to the ${department} Department!`);
    // In a real application, you could redirect to a specific page:
    // window.location.href = `/${department.toLowerCase()}-department.html`;
}
